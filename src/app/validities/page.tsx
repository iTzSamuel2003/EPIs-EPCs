"use client";

import { useEffect, useMemo, useState } from "react";
import { CalendarClock, LoaderCircle, Search, ShieldCheck, UserRound, X } from "lucide-react";
import { createClient } from "@/lib/supabase/client";

type Material = { id: string; name: string; internal_code: string; type: "EPI" | "EPC" | "FERRAMENTAL"; unit: string };
type Lot = { id: string; lot_number: string; available_quantity: number; expires_at: string | null; material_id: string; variant_id: string | null };
type Variant = { id: string; material_id: string; name: string; size: string | null };
type DeliveryItem = { id: string; lot_id: string | null; material_id: string; variant_id: string | null; quantity: number; delivery_id: string };
type Delivery = { id: string; employee_id: string };
type Employee = { id: string; full_name: string; registration: string | null };
type ReturnItem = { delivery_item_id: string; quantity: number };
type InventoryRow = { id: string; material: Material | null; variant: Variant | null; lot_number: string; expires_at: string | null; quantity: number; location: "Estoque" | "Com funcionário"; employee: Employee | null };

function daysUntil(value: string | null) { if (!value) return null; return Math.ceil((new Date(`${value}T00:00:00`).getTime() - new Date(new Date().toDateString()).getTime()) / 86400000); }

export default function ValiditiesPage() {
  const [rows, setRows] = useState<InventoryRow[]>([]); const [query, setQuery] = useState(""); const [filter, setFilter] = useState("all"); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [alertDays, setAlertDays] = useState(30);
  useEffect(() => { void Promise.resolve().then(async () => {
    const supabase = createClient();
    const [{ data: materialData, error: materialError }, { data: lotData, error: lotError }, { data: variantData, error: variantError }, { data: itemData, error: itemError }, { data: deliveryData, error: deliveryError }, { data: employeeData, error: employeeError }, { data: returnData, error: returnError }, { data: organizationData, error: organizationError }] = await Promise.all([
      supabase.from("materials").select("id,name,internal_code,type,unit").eq("status", "active"),
      supabase.from("material_lots").select("id,lot_number,available_quantity,expires_at,material_id,variant_id"),
      supabase.from("material_variants").select("id,material_id,name,size").eq("active", true),
      supabase.from("delivery_items").select("id,lot_id,material_id,variant_id,quantity,delivery_id"),
      supabase.from("deliveries").select("id,employee_id"),
      supabase.from("employees").select("id,full_name,registration"),
      supabase.from("return_items").select("delivery_item_id,quantity"),
      supabase.from("organizations").select("validity_alert_days").single()
    ]);
    const loadError = materialError ?? lotError ?? variantError ?? itemError ?? deliveryError ?? employeeError ?? returnError ?? organizationError;
    if (loadError) { setError(loadError.message); setLoading(false); return; }
    const materials = new Map((materialData ?? []).map((item) => [item.id, item as Material])); const lots = (lotData ?? []) as Lot[]; const lotById = new Map(lots.map((lot) => [lot.id, lot])); const variants = new Map((variantData ?? []).map((item) => [item.id, item as Variant])); const deliveries = new Map((deliveryData ?? []).map((item) => [item.id, item as Delivery])); const employees = new Map((employeeData ?? []).map((item) => [item.id, item as Employee])); const returned = new Map<string, number>();
    for (const item of (returnData ?? []) as ReturnItem[]) returned.set(item.delivery_item_id, (returned.get(item.delivery_item_id) ?? 0) + Number(item.quantity));
    const inventory: InventoryRow[] = [];
    for (const lot of lots) if (Number(lot.available_quantity) > 0) inventory.push({ id: `stock-${lot.id}`, material: materials.get(lot.material_id) ?? null, variant: lot.variant_id ? variants.get(lot.variant_id) ?? null : null, lot_number: lot.lot_number, expires_at: lot.expires_at, quantity: Number(lot.available_quantity), location: "Estoque", employee: null });
    for (const item of (itemData ?? []) as DeliveryItem[]) { const quantity = Number(item.quantity) - (returned.get(item.id) ?? 0); if (quantity <= 0) continue; const lot = item.lot_id ? lotById.get(item.lot_id) : undefined; const delivery = deliveries.get(item.delivery_id); inventory.push({ id: `assigned-${item.id}`, material: materials.get(item.material_id) ?? null, variant: item.variant_id ? variants.get(item.variant_id) ?? null : lot?.variant_id ? variants.get(lot.variant_id) ?? null : null, lot_number: lot?.lot_number ?? "—", expires_at: lot?.expires_at ?? null, quantity, location: "Com funcionário", employee: delivery ? employees.get(delivery.employee_id) ?? null : null }); }
    setRows(inventory); setAlertDays(Math.max(0, Number(organizationData?.validity_alert_days ?? 30))); setLoading(false);
  }); }, []);
  const filtered = useMemo(() => rows.filter((row) => { const days = daysUntil(row.expires_at); const text = `${row.material?.name ?? ""} ${row.material?.internal_code ?? ""} ${row.variant?.name ?? ""} ${row.variant?.size ?? ""} ${row.lot_number} ${row.employee?.full_name ?? ""} ${row.employee?.registration ?? ""}`.toLowerCase(); const matchesQuery = text.includes(query.toLowerCase()); const matchesFilter = filter === "all" || (filter === "expired" && days !== null && days < 0) || (filter === "urgent" && days !== null && days >= 0 && days <= alertDays) || (filter === "ok" && (days === null || days > alertDays)); return matchesQuery && matchesFilter; }), [rows, query, filter, alertDays]);
  const counts = { expired: rows.filter((row) => (daysUntil(row.expires_at) ?? 9999) < 0).length, urgent: rows.filter((row) => { const days = daysUntil(row.expires_at); return days !== null && days >= 0 && days <= alertDays; }).length, without: rows.filter((row) => !row.expires_at).length };
  return <main className="module-shell validities-page"><header className="module-header"><div><p className="eyebrow">CONTROLE OPERACIONAL</p><h1>Validades e localização</h1><p className="module-subtitle">Acompanhe validade, quantidade, numeração e onde cada material está.</p></div></header>{error && <div className="feedback error-feedback"><X size={17} /> {error}</div>}<section className="module-toolbar"><div className="module-search"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar material, tamanho, lote ou funcionário" /></div><select value={filter} onChange={(event) => setFilter(event.target.value)}><option value="all">Todas as validades</option><option value="expired">Vencidos</option><option value="urgent">Até {alertDays} dias</option><option value="ok">Mais de {alertDays} dias / sem data</option></select></section><section className="module-summary"><div><strong>{rows.length}</strong><span>itens localizados</span></div><div><strong>{counts.expired}</strong><span>vencidos</span></div><div><strong>{counts.urgent}</strong><span>até {alertDays} dias</span></div><div><strong>{counts.without}</strong><span>sem validade</span></div></section><section className="panel module-table-card"><div className="panel-header"><div><h2>Materiais por localização</h2><p>{filtered.length} item(ns) listado(s), incluindo estoque e materiais entregues.</p></div></div>{loading ? <div className="module-loading"><LoaderCircle className="spin" size={22} /> Carregando localização dos materiais...</div> : filtered.length ? <div className="table-wrap"><table><thead><tr><th>MATERIAL</th><th>TAMANHO</th><th>QUANTIDADE</th><th>LOCALIZAÇÃO</th><th>VALIDADE</th><th>SITUAÇÃO</th></tr></thead><tbody>{filtered.map((row) => { const days = daysUntil(row.expires_at); const expired = days !== null && days < 0; const urgent = days !== null && days >= 0 && days <= alertDays; const label = days === null ? "Sem data" : expired ? `Vencido há ${Math.abs(days)} dia(s)` : days === 0 ? "Vence hoje" : `Vence em ${days} dia(s)`; return <tr key={row.id}><td><div className="material-cell"><div className={`material-type-icon ${row.material?.type === "EPC" ? "epc" : "epi"}`}><CalendarClock size={17} /></div><div><strong>{row.material?.name ?? "Material removido"}</strong><small>{row.material?.internal_code || "Código não informado"} · {row.material?.type}</small></div></div></td><td>{row.variant ? <><strong>{row.variant.size || row.variant.name}</strong>{row.variant.size && row.variant.name !== row.variant.size && <small>{row.variant.name}</small>}</> : "—"}</td><td><strong>{row.quantity}</strong> {row.material?.unit}</td><td>{row.location === "Estoque" ? <span className="status-pill success">Estoque</span> : <div className="material-cell"><UserRound size={17} /><span><strong>{row.employee?.full_name ?? "Funcionário não identificado"}</strong><small>{row.employee?.registration ? `Matrícula ${row.employee.registration}` : "Matrícula não informada"}</small></span></div>}</td><td>{row.expires_at ? new Date(`${row.expires_at}T00:00:00`).toLocaleDateString("pt-BR") : <span className="muted-cell">Não informado</span>}</td><td><span className={`status-pill ${expired ? "danger" : urgent ? "warning" : row.expires_at ? "success" : "warning"}`}>{label}</span></td></tr>; })}</tbody></table></div> : <div className="empty-state"><ShieldCheck size={27} /><strong>Nenhum item encontrado</strong><span>Não há materiais para os filtros selecionados.</span></div>}</section></main>;
}
