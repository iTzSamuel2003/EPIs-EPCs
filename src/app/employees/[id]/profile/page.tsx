"use client";

import { FormEvent, useEffect, useState } from "react";
import { ArrowLeft, Check, Plus, Save, Trash2, X } from "lucide-react";
import Link from "next/link";
import { useParams } from "next/navigation";
import { createClient } from "@/lib/supabase/client";

type Profile = { shirt_size: string; pants_size: string; shoe_size: string; helmet_size: string; glove_size: string; uniform_notes: string; completed_by_employee: boolean };
type Course = { id: string; name: string; provider: string; completed_at: string; expires_at: string; certificate_number: string; notes: string };
const emptyProfile: Profile = { shirt_size: "", pants_size: "", shoe_size: "", helmet_size: "", glove_size: "", uniform_notes: "", completed_by_employee: false };
const emptyCourse = { name: "", provider: "", completed_at: "", expires_at: "", certificate_number: "", notes: "" };

export default function EmployeeProfilePage() {
  const { id } = useParams<{ id: string }>();
  const [name, setName] = useState(""); const [profile, setProfile] = useState<Profile>(emptyProfile); const [courses, setCourses] = useState<Course[]>([]); const [course, setCourse] = useState(emptyCourse); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const [success, setSuccess] = useState("");
  useEffect(() => { void load(); }, [id]);
  async function load() { const supabase = createClient(); const [{ data: employee }, { data: profileData }, { data: courseData, error: courseError }] = await Promise.all([supabase.from("employees").select("full_name").eq("id", id).single(), supabase.from("employee_profiles").select("shirt_size,pants_size,shoe_size,helmet_size,glove_size,uniform_notes,completed_by_employee").eq("employee_id", id).maybeSingle(), supabase.from("employee_courses").select("id,name,provider,completed_at,expires_at,certificate_number,notes").eq("employee_id", id).order("expires_at", { ascending: true, nullsFirst: false })]); if (courseError) setError(courseError.message); setName(employee?.full_name ?? ""); setProfile({ ...emptyProfile, ...(profileData ?? {}) }); setCourses((courseData ?? []) as Course[]); setLoading(false); }
  function update(field: keyof Profile, value: string | boolean) { setProfile((current) => ({ ...current, [field]: value })); }
  async function saveProfile(event: FormEvent) { event.preventDefault(); setSaving(true); setError(""); setSuccess(""); const supabase = createClient(); const { data: auth } = await supabase.auth.getUser(); const { data: userProfile } = await supabase.from("profiles").select("organization_id").eq("id", auth.user?.id ?? "").single(); const { error: saveError } = await supabase.from("employee_profiles").upsert({ employee_id: id, organization_id: userProfile?.organization_id, ...profile, updated_at: new Date().toISOString() }); if (saveError) setError(saveError.message); else setSuccess("Medidas atualizadas com sucesso."); setSaving(false); }
  async function addCourse(event: FormEvent) { event.preventDefault(); if (!course.name.trim()) return; setError(""); const supabase = createClient(); const { data: auth } = await supabase.auth.getUser(); const { data: userProfile } = await supabase.from("profiles").select("organization_id").eq("id", auth.user?.id ?? "").single(); const { error: saveError } = await supabase.from("employee_courses").insert({ ...course, employee_id: id, organization_id: userProfile?.organization_id, name: course.name.trim(), provider: course.provider.trim() || null, certificate_number: course.certificate_number.trim() || null, notes: course.notes.trim() || null, completed_at: course.completed_at || null, expires_at: course.expires_at || null }); if (saveError) setError(saveError.message); else { setCourse(emptyCourse); await load(); setSuccess("Curso adicionado Ã  ficha."); } }
  async function removeCourse(courseId: string) { const { error: removeError } = await createClient().from("employee_courses").delete().eq("id", courseId); if (removeError) setError(removeError.message); else setCourses((current) => current.filter((item) => item.id !== courseId)); }
  if (loading) return <main className="module-shell"><div className="module-loading">Carregando perfil...</div></main>;
  return <main className="module-shell"><header className="module-header"><div><p className="eyebrow">PERFIL DO COLABORADOR</p><h1>Medidas e cursos</h1><p className="module-subtitle">Dados usados automaticamente na ficha de materiais de {name}.</p></div><Link className="secondary-button" href={`/employees/${id}`}><ArrowLeft size={16} /> Voltar Ã  ficha</Link></header>{success && <div className="feedback success-feedback"><Check size={17} /> {success}</div>}{error && <div className="feedback error-feedback"><X size={17} /> {error}</div>}<section className="panel edit-employee-card"><form className="material-form" onSubmit={saveProfile}><div className="form-section-title"><h2>Tamanhos e medidas</h2><p>Preencha uma vez para orientar entregas de uniforme e equipamentos.</p></div><div className="form-grid three"><label>Camiseta / uniforme<input value={profile.shirt_size} onChange={(e) => update("shirt_size", e.target.value)} placeholder="Ex.: M" /></label><label>CalÃ§a<input value={profile.pants_size} onChange={(e) => update("pants_size", e.target.value)} placeholder="Ex.: 42" /></label><label>CalÃ§ado / botina<input value={profile.shoe_size} onChange={(e) => update("shoe_size", e.target.value)} placeholder="Ex.: 40" /></label></div><div className="form-grid two"><label>Capacete<input value={profile.helmet_size} onChange={(e) => update("helmet_size", e.target.value)} placeholder="Ex.: 58" /></label><label>Luvas<input value={profile.glove_size} onChange={(e) => update("glove_size", e.target.value)} placeholder="Ex.: 9 / G" /></label></div><label>ObservaÃ§Ãµes de medidas<textarea rows={3} value={profile.uniform_notes} onChange={(e) => update("uniform_notes", e.target.value)} placeholder="PreferÃªncias ou medidas especiais" /></label><label className="checkbox-line"><input type="checkbox" checked={profile.completed_by_employee} onChange={(e) => update("completed_by_employee", e.target.checked)} /> Dados preenchidos pelo prÃ³prio colaborador</label><div className="modal-actions"><button className="primary-button" disabled={saving}>{saving ? "Salvando..." : "Salvar medidas"}<Save size={16} /></button></div></form></section><section className="panel edit-employee-card"><div className="form-section-title"><h2>Cursos e treinamentos</h2><p>Registre capacitaÃ§Ãµes e vencimentos para consulta na ficha.</p></div><form className="material-form" onSubmit={addCourse}><div className="form-grid three"><label>Nome do curso<input value={course.name} onChange={(e) => setCourse({ ...course, name: e.target.value })} placeholder="NR-10, NR-35..." required /></label><label>InstituiÃ§Ã£o<input value={course.provider} onChange={(e) => setCourse({ ...course, provider: e.target.value })} /></label><label>Certificado<input value={course.certificate_number} onChange={(e) => setCourse({ ...course, certificate_number: e.target.value })} /></label></div><div className="form-grid two"><label>ConclusÃ£o<input type="date" value={course.completed_at} onChange={(e) => setCourse({ ...course, completed_at: e.target.value })} /></label><label>Validade<input type="date" value={course.expires_at} onChange={(e) => setCourse({ ...course, expires_at: e.target.value })} /></label></div><button className="secondary-button"><Plus size={16} /> Adicionar curso</button></form><div className="table-wrap"><table><thead><tr><th>CURSO</th><th>INSTITUIÃ‡ÃƒO</th><th>VALIDADE</th><th>AÃ‡Ã•ES</th></tr></thead><tbody>{courses.map((item) => <tr key={item.id}><td><strong>{item.name}</strong><small>{item.certificate_number || "Sem certificado informado"}</small></td><td>{item.provider || "â€”"}</td><td>{item.expires_at ? new Date(`${item.expires_at}T00:00:00`).toLocaleDateString("pt-BR") : "â€”"}</td><td><button className="action-button" onClick={() => void removeCourse(item.id)}><Trash2 size={14} /> Remover</button></td></tr>)}</tbody></table>{!courses.length && <div className="empty-state"><strong>Nenhum curso cadastrado</strong><span>Adicione os treinamentos obrigatÃ³rios ou complementares.</span></div>}</div></section></main>;
}

