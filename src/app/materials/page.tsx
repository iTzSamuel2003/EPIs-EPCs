"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { Boxes, Check, ChevronDown, LoaderCircle, Pencil, Plus, Search, ShieldCheck, X } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
import type { MaterialType } from "@/types/domain";

type MaterialRow = { id: string; internal_code: string; name: string; type: MaterialType; unit: string; ca_number: string | null; minimum_stock: number; location: string | null; status: "active" | "inactive" };
type MaterialForm = { internal_code: string; name: string; type: MaterialType; unit: string; description: string; brand: string; manufacturer: string; model: string; ca_number: string; ca_expires_at: string; useful_life_months: string; minimum_stock: string; location: string; notes: string };
const emptyForm: MaterialForm = { internal_code: "", name: "", type: "EPI", unit: "un.", description: "", brand: "", manufacturer: "", model: "", ca_number: "", ca_expires_at: "", useful_life_months: "", minimum_stock: "0", location: "", notes: "" };

function statusFor(material: MaterialRow, stock: number) { if (material.status === "inactive") return ["Inativo", "inactive"]; if (stock === 0) return ["Sem estoque", "danger"]; if (stock <= material.minimum_stock) return ["Estoque baixo", "warning"]; return ["Normal", "success"]; }

export default function MaterialsPage() {
  const [materials, setMaterials] = useState<MaterialRow[]>([]); const [stockByMaterial, setStockByMaterial] = useState<Record<string, number>>({}); const [form, setForm] = useState<MaterialForm>(emptyForm); const [query, setQuery] = useState(""); const [typeFilter, setTypeFilter] = useState("all"); const [statusFilter, setStatusFilter] = useState("all"); const [lowStockOnly, setLowStockOnly] = useState(false); const [showForm, setShowForm] = useState(false); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); const [success, setSuccess] = useState("");

  async function loadMaterials() { setLoading(true); const supabase = createClient(); const { data, error: materialsError } = await supabase.from("materials").select("id, internal_code, name, type, unit, ca_number, minimum_stock, location, status").order("name"); if (materialsError) { setError(materialsError.message); setLoading(false); return; } const { data: lots, error: lotsError } = await supabase.from("material_lots").select("material_id, available_quantity"); if (lotsError) { setError(lotsError.message); setLoading(false); return; } const totals = (lots ?? []).reduce<Record<string, number>>((acc, lot) => { acc[lot.material_id] = (acc[lot.material_id] ?? 0) + (lot.available_quantity ?? 0); return acc; }, {}); setMaterials((data ?? []) as MaterialRow[]); setStockByMaterial(totals); setLoading(false); }
  useEffect(() => { void Promise.resolve().then(() => loadMaterials()); }, []);
  const filtered = useMemo(() => materials.filter((material) => { const stock = stockByMaterial[material.id] ?? 0; return `${material.name} ${material.internal_code} ${material.ca_number ?? ""}`.toLowerCase().includes(query.toLowerCase()) && (typeFilter === "all" || material.type === typeFilter) && (statusFilter === "all" || material.status === statusFilter) && (!lowStockOnly || stock <= material.minimum_stock); }), [materials, stockByMaterial, query, typeFilter, statusFilter, lowStockOnly]);
  function updateField(field: keyof MaterialForm, value: string) { setForm((current) => ({ ...current, [field]: value })); }
  async function saveMaterial(event: FormEvent) { event.preventDefault(); setError(""); setSuccess(""); if (form.type === "EPI" && !form.ca_number.trim()) { setError("O número do CA é obrigatório para materiais do tipo EPI."); return; } setSaving(true); const supabase = createClient(); const { data: auth } = await supabase.auth.getUser(); if (!auth.user) { setError("Sua sessão expirou. Entre novamente."); setSaving(false); return; } const { data: profile } = await supabase.from("profiles").select("organization_id").eq("id", auth.user.id).single(); if (!profile?.organization_id) { setError("Não foi possível identificar a organização do usuário."); setSaving(false); return; } const { error: insertError } = await supabase.from("materials").insert({ organization_id: profile.organization_id, internal_code: form.internal_code.trim(), name: form.name.trim(), type: form.type, unit: form.unit.trim() || "un.", description: form.description || null, brand: form.brand || null, manufacturer: form.manufacturer || null, model: form.model || null, ca_number: form.type === "EPI" ? form.ca_number.trim() : null, ca_expires_at: form.ca_expires_at || null, useful_life_months: form.useful_life_months ? Number(form.useful_life_months) : null, minimum_stock: Number(form.minimum_stock) || 0, location: form.location || null, notes: form.notes || null, status: "active" }); if (insertError) setError(insertError.code === "23505" ? "Já existe um material com este código interno." : insertError.message); else { setSuccess("Material cadastrado com sucesso."); setForm(emptyForm); setShowForm(false); await loadMaterials(); } setSaving(false); }

  async function editMaterial(material: MaterialRow) { const name = window.prompt("Nome do material", material.name); if (name === null || !name.trim()) return; const minimum = window.prompt("Estoque mínimo", String(material.minimum_stock)); const { error: updateError } = await createClient().from("materials").update({ name: name.trim(), minimum_stock: Number(minimum) || 0 }).eq("id", material.id); if (updateError) setError(updateError.message); else { setSuccess("Material atualizado com sucesso."); await loadMaterials(); } }
  return <main className="module-shell"><header className="module-header"><div><p className="eyebrow">CADASTRO E CONTROLE</p><h1>Materiais</h1><p className="module-subtitle">Gerencie EPIs e EPCs, certificados e níveis mínimos de estoque.</p></div><button className="primary-button" onClick={() => { setShowForm(true); setSuccess(""); setError(""); }}><Plus size={17} /> Novo material</button></header>{success && <div className="feedback success-feedback"><Check size={17} /> {success}</div>}{error && !showForm && <div className="feedback error-feedback"><X size={17} /> {error}</div>}<section className="module-toolbar"><div className="module-search"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar por nome, código ou CA" /></div><select value={typeFilter} onChange={(event) => setTypeFilter(event.target.value)}><option value="all">Todos os tipos</option><option value="EPI">EPI</option><option value="EPC">EPC</option><option value="FERRAMENTAL">Ferramental</option></select><select value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)}><option value="all">Todos os status</option><option value="active">Ativos</option><option value="inactive">Inativos</option></select><label className="check-filter"><input type="checkbox" checked={lowStockOnly} onChange={(event) => setLowStockOnly(event.target.checked)} /> Estoque baixo</label></section><section className="module-summary"><div><strong>{materials.length}</strong><span>materiais cadastrados</span></div><div><strong>{materials.filter((item) => item.type === "EPI").length}</strong><span>EPIs</span></div><div><strong>{materials.filter((item) => item.type === "EPC").length}</strong><span>EPCs</span></div><div><strong>{materials.filter((item) => (stockByMaterial[item.id] ?? 0) <= item.minimum_stock && item.status === "active").length}</strong><span>estoque baixo</span></div></section><section className="panel module-table-card"><div className="panel-header"><div><h2>Catálogo de materiais</h2><p>{filtered.length} resultado(s) encontrados</p></div><button className="select-button">Ordenar por nome <ChevronDown size={15} /></button></div>{loading ? <div className="module-loading"><LoaderCircle className="spin" size={22} /> Carregando materiais...</div> : <div className="table-wrap"><table><thead><tr><th>MATERIAL</th><th>TIPO</th><th>CA</th><th>ESTOQUE</th><th>LOCALIZAÇÃO</th><th>SITUAÇÃO</th><th>AÇÕES</th></tr></thead><tbody>{filtered.map((material) => { const stock = stockByMaterial[material.id] ?? 0; const [status, tone] = statusFor(material, stock); return <tr key={material.id}><td><div className="material-cell"><div className={`material-type-icon ${material.type === "EPI" ? "epi" : "epc"}`}><Boxes size={17} /></div><div><strong>{material.name}</strong><small>{material.internal_code} · {material.unit}</small></div></div></td><td><span className={`type-badge ${material.type.toLowerCase()}`}>{material.type}</span></td><td>{material.ca_number || <span className="muted-cell">Não se aplica</span>}</td><td><strong>{stock}</strong> <span className="muted-cell">/ mín. {material.minimum_stock}</span></td><td>{material.location || <span className="muted-cell">Não informado</span>}</td><td><span className={`status-pill ${tone}`}>{status}</span></td><td><button className="action-button" onClick={() => void editMaterial(material)}><Pencil size={14} /> Editar</button></td></tr>; })}</tbody></table>{!filtered.length && <div className="empty-state"><ShieldCheck size={27} /><strong>Nenhum material encontrado</strong><span>Ajuste os filtros ou cadastre o primeiro material.</span></div>}</div>}</section>{showForm && <div className="modal-backdrop"><section className="modal-card"><div className="modal-header"><div><p className="eyebrow">NOVO CADASTRO</p><h2>Cadastrar material</h2></div><button className="close-modal" onClick={() => setShowForm(false)}><X size={19} /></button></div><form className="material-form" onSubmit={saveMaterial}><div className="form-grid two"><label>Código interno<input value={form.internal_code} onChange={(event) => updateField("internal_code", event.target.value)} placeholder="EPI-00001" required /></label><label>Nome do material<input value={form.name} onChange={(event) => updateField("name", event.target.value)} placeholder="Ex.: Capacete Classe B" required /></label></div><div className="form-grid three"><label>Tipo<select value={form.type} onChange={(event) => updateField("type", event.target.value as MaterialType)}><option value="EPI">EPI</option><option value="EPC">EPC</option></select></label><label>Unidade<input value={form.unit} onChange={(event) => updateField("unit", event.target.value)} placeholder="un." required /></label><label>Estoque mínimo<input type="number" min="0" value={form.minimum_stock} onChange={(event) => updateField("minimum_stock", event.target.value)} required /></label></div><div className="form-grid three"><label>Número do CA {form.type === "EPI" && <em>obrigatório</em>}<input value={form.ca_number} onChange={(event) => updateField("ca_number", event.target.value)} placeholder={form.type === "EPI" ? "Ex.: 42.781" : "Não se aplica"} required={form.type === "EPI"} /></label><label>Validade do CA<input type="date" value={form.ca_expires_at} onChange={(event) => updateField("ca_expires_at", event.target.value)} /></label><label>Vida útil (meses)<input type="number" min="0" value={form.useful_life_months} onChange={(event) => updateField("useful_life_months", event.target.value)} placeholder="Ex.: 12" /></label></div><div className="form-grid three"><label>Marca<input value={form.brand} onChange={(event) => updateField("brand", event.target.value)} placeholder="Marca" /></label><label>Fabricante<input value={form.manufacturer} onChange={(event) => updateField("manufacturer", event.target.value)} placeholder="Fabricante" /></label><label>Modelo<input value={form.model} onChange={(event) => updateField("model", event.target.value)} placeholder="Modelo" /></label></div><label>Localização no estoque<input value={form.location} onChange={(event) => updateField("location", event.target.value)} placeholder="Ex.: Almoxarifado A" /></label><label>Descrição<textarea value={form.description} onChange={(event) => updateField("description", event.target.value)} placeholder="Descrição e características do material" rows={2} /></label><label>Observações<textarea value={form.notes} onChange={(event) => updateField("notes", event.target.value)} placeholder="Observações internas" rows={2} /></label>{error && <p className="login-error">{error}</p>}<div className="modal-actions"><button type="button" className="secondary-button" onClick={() => setShowForm(false)}>Cancelar</button><button className="primary-button" disabled={saving}>{saving ? "Salvando..." : "Cadastrar material"}<Plus size={16} /></button></div></form></section></div>}</main>;
}
